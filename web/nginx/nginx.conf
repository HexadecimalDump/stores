server {
    listen 80;
    server_name localhost; # Or your domain name

    root /usr/share/nginx/html; # Path to your static files (including index.html)

    location / {
        try_files $uri $uri/ /index.html;
    }
}